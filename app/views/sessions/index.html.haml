%h3
  =t("sign_in_with")
%p
  -if session[:user_id]
    ==#{t('signed_in_as')} #{ current_user }
  -else
    =link_to image_tag("twitter_32.png"), "/auth/twitter"
    =link_to image_tag("google_32.png"), "/auth/google_oauth2"
